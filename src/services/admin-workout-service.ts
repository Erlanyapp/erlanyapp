import type { SupabaseClient } from "@supabase/supabase-js";import { createAdminWorkoutRepository } from "@/repositories/admin-workout-repository";import { assignmentInput,workoutExerciseInput,workoutId,workoutInput } from "@/domain/admin-workout";
export function createAdminWorkoutService(client:SupabaseClient){const r=createAdminWorkoutRepository(client);return {...r,save:(id:string|null,f:FormData)=>r.save(id,workoutInput(f)),toggle:(id:string,a:boolean)=>r.toggle(workoutId(id as never),a),addExercise:(id:string,f:FormData)=>r.addExercise(workoutId(id as never),workoutExerciseInput(f)),updateExercise:(id:string,f:FormData)=>r.updateExercise(id,workoutExerciseInput(f)),assign:(id:string,f:FormData)=>r.assign(workoutId(id as never),assignmentInput(f)),updateAssignment:(id:string,f:FormData)=>r.updateAssignment(id,assignmentInput(f))};}
